---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mizar-dropletd
  namespace: default
spec:
  selector:
    matchLabels:
      job: mizar-dropletd
  template:
    metadata:
      labels:
        job: mizar-dropletd
    spec:
      serviceAccountName: mizar-operator
      hostNetwork: true
      hostPID: true
      volumes:
        - name: mizar
          hostPath:
            path: /home
            type: Directory
      containers:
      - image: selgohari/dropletd:latest
        name: mizar-dropletd
        securityContext:
            privileged: true
        volumeMounts:
          - name: mizar
            mountPath: /mizar
